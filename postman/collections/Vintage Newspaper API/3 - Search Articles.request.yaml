$kind: http-request
name: 3 - Search Articles
url: http://localhost/vintage-newspaper/api/search.php?q=artificial
method: GET
queryParams:
  q: artificial
scripts:
  - type: afterResponse
    code: |-
      pm.test('Status 200', () => pm.response.to.have.status(200));
      pm.test('Returns search results', () => {
          const json = pm.response.json();
          pm.expect(json).to.be.an('object');
      });
    language: text/javascript
order: 3000
